/**
 * 📦 AuthForm
 * Renderiza métodos de inicio de sesión:
 * - Teléfono, correo, usuario
 * - OAuth (Google, Apple, etc.)
 */
export const AuthForm = () => {
  return (
    <div className="flex flex-col gap-4 bg-[var(--color-surface)] p-4 rounded-lg border border-[var(--color-border)]"></div>
  );
};
